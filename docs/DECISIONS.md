# Decision Log

Why this app is built the way it is, newest first. Kept verbatim, including the numbers
that settled each one — a rejected approach is only useful if the evidence against it
survives.

```
### YYYY-MM-DD — <the claim, stated as a conclusion>

Paragraphs, each opening with a bolded lead sentence, then the evidence: what was
measured, how many runs, on what machine, and what the spread was. A decision recorded
without its measurement is an opinion with a date on it.
```

Read this before re-attempting something recorded here as rejected. The experiment has
already been run, and re-running it costs a session that could have shipped something.

Current-state architecture belongs in the codebase and `CLAUDE.md`, not here. This file
is history: it explains why, not what.

_No entries yet._
